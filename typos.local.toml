# Repository-specific identifiers, quotations and deliberate fixtures.

schema = 1

[oxford]
stems = []

[words]
# Official names, abbreviations, and repository-specific terms: "Azure
# Architecture Center", the mold linker, Level of Detail (LOD), and a Spanish
# Gherkin example.
accepted = ["Center", "LOD", "Mold", "inventario", "mold"]

[words.corrections]

[patterns]
# Preserve externally defined API names, file names, and tool names without
# weakening prose spelling checks. These patterns match only exact code spans.
ignore = [
    "`(?:AXDescription|AXGroup|AXRole|AXLabel)`",
    "`Catalog(?:::[^`]+)?`",
    "`catalog\\.rs`",
    "`color`",
    "flavor = \"current_thread\"",
    "rust-analyzer",
]

[files]
exclude = []
