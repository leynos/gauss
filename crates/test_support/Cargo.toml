[package]
name = "test_support"
version = "0.1.0"
edition = "2024"

[dependencies]
gauss-core = { version = "0.1.0", path = "../gauss-core", features = ["test-support"] }
gpui = "0.2.2"
thiserror = "2.0.18"

[lints]
workspace = true
